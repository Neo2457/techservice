# Forzar instalación de devDependencies aunque NODE_ENV=production esté seteado.
# Necesario para que el build de TypeScript funcione en hostings (Hostinger, etc.)
# que setean NODE_ENV=production durante la instalación.
production=false
